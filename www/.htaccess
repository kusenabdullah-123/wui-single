<IfModule mod_rewrite.c>
  RewriteEngine On

  # Jangan rewrite file yang benar-benar ada
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Semua request lainnya diarahkan ke index.html
  RewriteRule ^ index.html [L]
</IfModule>
